# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'BrainPrint' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!
  pod 'SwiftyJSON', '~> 4.0'
  pod 'Alamofire'
  pod 'RxAlamofire'
  pod 'RxSwift'
  # Pods for Brain Print App

  target 'BrainPrintTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'BrainPrintUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
